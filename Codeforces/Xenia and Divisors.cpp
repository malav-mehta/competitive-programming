#include <iostream>
using namespace std;

int n;
int s[99999];

int main() {
    cin.sync_with_stdio(0);
    cin.tie(0);
    
    cin >> n;
    for (int i = 0; i < n; ++i)
        cin >> s[i];
    
    return 0;
}
